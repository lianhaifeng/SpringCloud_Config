spring:
   profiles:
      active:
      - dev
---
spring:
  profiles: dev
  application:
      name: SpringCloudConfig-test-dev
---
spring:
 profiles: default
 application:
      name: SpringCloudConfig-test-default